<div class="container">
  <h2>The {{house.name}} Household</h2>
  <hr>

  <h4>Your housemates</h4>
  <ul class="collection">
    {{#each house.members}}
    <li class="collection-item avatar">
      <img src="{{this.profile.picture}}" alt="" class="circle">
      <span class="title">{{this.profile.name}}</span>
      <p>{{this.email}} <br>
         {{this.phone}}
      </p>
      <a href="#!" class="secondary-content"><i class="material-icons">grade</i></a>
    </li>
    {{/each}}
  </ul>
  <hr>
  <h4>Your training images</h4>
  //TODO house training images on S3, on show here.
  //Give options to delete and show more.

</div>
